[package]
name = "maturity-macro"
description = "Procedural macros for annotating code maturity and stability."
keywords = ["maturity", "stability", "metrics", "attribute", "annotation"]
categories = ["development-tools"]
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
proc-macro = true

[dependencies]
