# maturity-report

This crate contains the core implementation behind the `maturity` tooling.

Its responsibilities lies in:

- project scanning
- file discovery
- rust syntax parsing
- metric collection
- report generation
- command execution
